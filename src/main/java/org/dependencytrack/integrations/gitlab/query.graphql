query {
  projects(membership: true, search: "", first: 10) {
    nodes {
      name
      fullPath
      maxAccessLevel {
        stringValue
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
