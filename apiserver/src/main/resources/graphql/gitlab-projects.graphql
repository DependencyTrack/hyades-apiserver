query ($cursor: String) {
  projects(archived: EXCLUDE, membership: true, first: 10, after: $cursor) {
    nodes {
      name
      fullPath
      maxAccessLevel {
        stringValue
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}