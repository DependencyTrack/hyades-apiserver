query ($cursor: String) {
  projects(membership: true, first: 10, after: $cursor) {
    nodes {
      name
      fullPath
      maxAccessLevel {
        stringValue
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}